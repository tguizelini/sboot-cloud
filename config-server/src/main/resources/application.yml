server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tguizelini/sboot-cloud-floricultura
          search-paths: fornecedor
