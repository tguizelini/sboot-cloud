server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tguizelini/sboot-cloud-floricultura/tree/master/config-repo
          search-paths: fornecedor
